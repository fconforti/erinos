#!/usr/bin/env bash
set -euo pipefail

# erinos-cli.sh — CLI subcommand dispatcher
# The canonical implementation lives in /usr/local/bin/erinos.
# This file exists as the source for the airootfs copy.

printf 'erinos-cli.sh: Use "erinos" instead.\n' >&2
exit 1
